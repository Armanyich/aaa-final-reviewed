Rule coverage integration tests live here when they need cross-analyzer fixtures
that are too broad for a single server-specific test module.

The directory is intentionally kept with this README so it does not look like an
unfinished placeholder when `__pycache__` happens to be present locally.
